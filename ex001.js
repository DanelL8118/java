const prompt = require('prompt-sync')();

let espaço = prompt('digite quanto espaço já foi usado: ')

console.log('\nEspaço total: 100 GB\nEspaço usado:', espaço, 'GB\nespaço disponivel:', total = 100-espaço, 'GB')